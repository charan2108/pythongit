# creating list sing function
# list() is apredefined function  in pythons
# list() function is used to convert the other sequences or data types into list data types
# predefined class in python
# once we create a list internally it can be internally represented as a list class object type
a = range(0, 21)
b = list(a)
print(b)
print(type(b))
